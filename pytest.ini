[pytest]
markers =
    p1: P1 emergent-kernel and orbit validation tests
    p2: P2 weak-lensing and convergence validation tests
    p3: P3 black-hole prediction prototype tests
    validation: repository validation and artifact consistency checks
    physics: physics-facing checks over generated/reference artifacts
addopts = -ra
